import 'package:flutter/material.dart';

class SpilnotaDetailPage extends StatelessWidget {
  const SpilnotaDetailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Деталі спільноти'),
      ),
      body: Center(
        child: Text('Деталі спільноти'),
      ),
    );
  }
}
